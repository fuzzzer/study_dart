//Write a lambda function that takes a list of tuples as input,
// where each tuple contains two strings, and returns a new list containing only the tuples where the
// length of the first string is greater than the length of the second string.
